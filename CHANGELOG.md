# Changelog

All notable changes to `@haruhime/compliance`. Versions follow [semver](https://semver.org): a rule or data change that can change a verdict is a minor version while we're on 0.x.

## 0.1.0 (unreleased)

- First release. Extracted from packs.haruhime.moe's tournament check.
- `evaluateBeatmapset`, `factsFromOsuBeatmapset`, `verdictText` and the text constants, `isLeaderboardStatus`, `UPSTREAM`, `RULE_LINKS`, and the types.
- Rules and data from hburn7/omc-api at `bb356b3` (2026-06-28).
